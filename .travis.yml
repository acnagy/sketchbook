sudo: false

language: java
jdk: openjdk7

addons:  
  apt: 
    sources: 
      - deadsnakes
    packages:
      - python3.5

install: true

script:
  - python --version
  - unlink /usr/bin/python
  - ln -s /usr/bin/python3 /usr/bin/python
  - python --version

dist: precise

os: linux

