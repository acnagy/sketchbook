sudo: true

language: java
jdk: openjdk7

addons:  
  apt: 
    sources: 
      - deadsnakes
    packages:
      - python3.5

install: true

script:
  - python --version
  - python3 --version

dist: precise

os: linux

